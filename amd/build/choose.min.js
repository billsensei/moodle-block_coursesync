define("block_coursesync/choose",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0;
/**
   * Select all / select none for the "Choose what to copy" checkbox picker.
   *
   * "Select all" ticks only what is new or changed (data-coursesync-bulk).
   * Ticking something already in the course copies it again, so that is only
   * ever done one row at a time. "Select none" clears everything.
   *
   * Also stops the form being sent twice: a second click on a slow sync would
   * otherwise start a second run, which the server refuses (syncer::run()'s
   * lock) - and it is that refusal the browser would then show.
   *
   * @module     block_coursesync/choose
   * @copyright  2026 Course Sync project
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const SELECTORS_FORM="#coursesync-choose",SELECTORS_SELECTALL='[data-action="coursesync-select-all"]',SELECTORS_SELECTNONE='[data-action="coursesync-select-none"]',SELECTORS_SUBMIT='input[type="submit"], button[type="submit"]',SELECTORS_BULK_CHECKBOX='input[type="checkbox"][data-coursesync-bulk]:not(:disabled)',SELECTORS_ANY_CHECKBOX='input[type="checkbox"]:not(:disabled)',setAll=(form,selector,checked)=>{form.querySelectorAll(selector).forEach((checkbox=>{checkbox.checked=checked}))};_exports.init=()=>{const form=document.querySelector(SELECTORS_FORM);form&&(form.addEventListener("click",(e=>{e.target.closest(SELECTORS_SELECTALL)?(e.preventDefault(),setAll(form,SELECTORS_BULK_CHECKBOX,!0)):e.target.closest(SELECTORS_SELECTNONE)&&(e.preventDefault(),setAll(form,SELECTORS_ANY_CHECKBOX,!1))})),form.addEventListener("submit",(e=>{form.dataset.submitted?e.preventDefault():(form.dataset.submitted="1",form.querySelectorAll(SELECTORS_SUBMIT).forEach((button=>{button.disabled=!0})))})))}}));

//# sourceMappingURL=choose.min.js.map